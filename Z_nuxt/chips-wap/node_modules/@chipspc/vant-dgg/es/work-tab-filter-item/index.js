// Utils
import { createNamespace } from '../utils'; // Mixins

import { ChildrenMixin } from '../mixins/relation';

var _createNamespace = createNamespace('work-tab-filter-item'),
    createComponent = _createNamespace[0],
    bem = _createNamespace[1];

export default createComponent({
  mixins: [ChildrenMixin('SpWorkTabFilter')],
  props: {},
  data: function data() {
    return {
      active: false
    };
  },
  computed: {},
  methods: {
    handleItemClick: function handleItemClick() {
      this.parent.onChange(this.slots()[0].text, this.name || this.index);
      this.parent.itemIndex = this.index;
    }
  },
  render: function render() {
    var h = arguments[0];
    return h("div", {
      "class": bem({
        active: this.active
      }),
      "on": {
        "click": this.handleItemClick
      }
    }, [h("div", {
      "class": bem('item')
    }, [this.slots(), h("span", {
      "class": bem('span')
    })])]);
  }
});